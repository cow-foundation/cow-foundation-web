{
  "name": "cow-foundation-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "apply-patches": "./scripts/apply-patches.sh",
    "dev": "yarn apply-patches && cd vendor/cowswap && yarn start:cowfi",
    "build": "yarn apply-patches && cd vendor/cowswap && yarn install && cd apps/cow-fi && npx next build",
    "clean": "git -C vendor/cowswap checkout -- apps/cow-fi/ && git -C vendor/cowswap checkout -- libs/"
  },
  "devDependencies": {
    "next": "15.2.4",
    "pino-pretty": "^13.0.0"
  }
}